#failed-card
  .card
    .card-header
      .failed
        %h1 FAILED!
    .card-body.text-center
      - if params[:type] == 'email'
        Email Verification Failed with following error:
      - else
        Transaction failed with following error:
      %br
      = params[:error_description]
